module Main

extern println = "fmt.Println" (s: string) ()

let cheesy = (item, item2) {
	# more test
    item ++ " and " ++ item2 ++ " with cheese"
}

let tester = (a) {

	let result = a + 1

	if result > 5 {
		a + 1
	} else {
		a + 100
	}
}

let main = {
	let something = {
		4 + 9
	}
	let a = something()
	let yumPizza = cheesy("pineapple", ("bbq" ++ "sauce"))
	println(yumPizza)
	# some comment
	let five = 1 / 1 + 3 * (55 - 2)
	# let mmm = 1 + 1
}
