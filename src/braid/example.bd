# test
let cheesy = func item item2 {
	let b = 5 + 6 - 1
	let c = [5, 6, 7]
    # more test
    item ++ " and " ++ item2 ++ " with cheese"
}

let tester = func a {
    let c = true
	let nothing = a + 1
	if a > 5 {
		a + 1
	} else {
		a + 100
	}
}

let main = func {
	let something = func {
		4 + 9
	}
	let a = 5.0 *. (4.0 +. 6.0) *. 2.0
	let yumPizza = cheesy("pineapple", ("bbq" ++ "sauce"))
	# hoo boy this is a good'un
	let five = 1 / 1 + 3 * (55 - 2)
	# let mmm = 1 + 1
}

