module Main

let number = {
    5.0 + 5.0
}

let broken = {
    let result = if true {
        "hello"
    } else {
        "never happens"

    }
}

let main = {
	// one
	let a = 2.0 * 5.0 + number()
	let f = broken() ++ "human"
	// two
	let b = 3 * -2 + 20 > 5
	let c = b
	let d = [5, 6]
	let e = b
}

