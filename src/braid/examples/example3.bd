module Main

let add = (one, two) {
	// one
	two ++ one
}

let things = {
	["kittens", "popcorn", "a turnip"]
}

let index = {
	0 + 1
}

let main = {
    let two = add("one","two")
	let three = things()
	let four = three[index()]
}
