module Main

let add = (one, two) {
	// one
	two ++ one
}

let things = {
	["kittens", "popcorn", "a turnip"]
}

let main = {
    let two = add("one","two")
	let three = things()
	let four = three[0]
}
