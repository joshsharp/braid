module Main

type StringResult = 
	| OkS string
	| ErrorS string

type Result ('a, 'b) = 
	| Ok 'a
	| Error 'b

type Person = {
	name: string,
	age: int64,
}

extern func println = "fmt.Println" (s: string) -> ()

let main = {
	let mm = Ok("Delicious")
	let ugh = ErrorS(500)
	let josh = Person{name: "Josh", age: 99}
	println(josh.name)
}

