module Main

extern func println = "fmt.Println" (s: string) -> ()

type StringResult = 
	| OkS string string
	| ErrorS string

type Result ('a, 'b) = 
	| Ok 'a
	| Error 'b

type Person = {
	name: string,
	age: int64,
}

let main = {
	let mm = Ok("Delicious")
	let meh = Error(0)
	let ugh = ErrorS("Bad smell")
	let josh = Person{name: "Josh", age: 99}
	println(josh.name)
}

