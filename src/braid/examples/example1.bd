module Main

let add = (a, b) {
	a + b
}

let cheesy = (item, item2) {
    item ++ " and " ++ item2 ++ " with cheese"
}

let main = {
	// one
	let a = 2.0
	// two
	let b = 3.0 +. -2.0
	let c = a + b
	let d = [5, 6]
	let e = b
	let f = ["things", "stuff"]
	let _ = add(4, 5)
	let pizza = cheesy("pizza", "bread")
	//Mod.f()
}

