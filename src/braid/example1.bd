module Main

let add = (a: int, b: int) int {
	a + b
}

let cheesy = (item, item2) {
    item ++ " and " ++ item2 ++ " with cheese"
}

let main = {
	# one
	let a = 2
	# two
	let b = 3 + -2
	let c = a + b
	let d = [5, 6]
	let e = b
	let _ = add(4, 5)
	let pizza = cheesy("pizza")
	#Mod.f()
}

